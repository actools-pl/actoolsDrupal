# Runtime Authority Map

> **Recorded at phase P0-A** (doc-only) by adopting the verified synthesis
> `00_reference/actools-phase0-implementation-plan.md` (= `design/actools-phase0-implementation-plan.md`).
> Every "Evidence" cell below was **spot-verified read-only** against the uploaded repo
> export `actoolsDrupal-main` during P0-A. No runtime file was modified. The synthesis
> was *adopted, not re-derived* (per the P0-A phase file, step 0).
>
> Base reference: the plan was built against git `839d3c8`; the operator must record the
> actual `main` HEAD when applying (the export carried no `.git`). P0-A changes only
> `docs/`+`design/` content, so this map is not base-sensitive.

## Purpose

This file records which source file is authoritative for each runtime concern. It prevents
future AI windows from editing orphaned or parallel code. **Every phase must update this map
if it changes which file is authoritative for any runtime behaviour.**

## Status legend

- `current` — the file that actually runs today on the live install path.
- `parallel` — a second live implementation of the same concern (a split that must be collapsed).
- `orphan` — present in the repo but sourced/called by nobody on the live path (reference only).
- `target` — where the concern is meant to live after Phase 0 (per LOCKED §2/§5/§6).
- `unknown` — not yet established.

A row may carry a compound status (e.g. `current (monolithic) → target via dispatcher; orphan twin exists`).

## Current-state map (verified)

| Runtime concern | Current authority (verified) | Parallel / orphan copy (verified) | Target authority (LOCKED) | Status |
|---|---|---|---|---|
| **Bootstrap variables + helpers** | `actools.sh` — `INSTALL_DIR` is `BASH_SOURCE`-relative (`actools.sh:94`), `ENV_FILE="$INSTALL_DIR/actools.env"` (`actools.sh:98`); `set -euo pipefail` (`:38`), `IFS=$'\n\t'` (`:39`), `ERR` trap (`:41`). **(P0-K)** the five bootstrap helper functions (`log`/`warn`/`error`/`section`/`dryrun`) are now sourced from `core/bootstrap.sh` (live module, **functions only** — extracted verbatim from the inline v14 block); the `DRY_RUN` init stays inline | none — the **v9.2 orphan is retired (P0-K)**: `core/bootstrap.sh` was overwritten with the live v14 helpers; its `export INSTALL_DIR="$REAL_HOME"`, `$REAL_HOME`-anchored `ENV_FILE`/`STATE_FILE`/`LOG_*`, `ACTOOLS_VERSION="9.2"` and its own lock/exec-redirect logic did **not** survive. Path/variable authority stays in `actools.sh` (guarded by `tests/core/bootstrap_test.bats` statics) | One bootstrap, one path semantics, one version string (`BASH_SOURCE`-relative) — LOCKED-§6 item via WP-0(c)/P0-F + WP-7/P0-B·J | **landed (P0-K)** — variables/paths = `actools.sh` (unchanged); helper functions = `core/bootstrap.sh` (**live module**, on the source-closure the live-authority guard checks). The `$REAL_HOME` variant is dead |
| **Stateless core functions (state / secrets / validate)** | `core/state.sh` (`init_state`/`set_state`/`get_state`/`is_installed`/`mark_installed`), `core/secrets.sh` (`rand_pass`/`gen_if_empty`/`get_db_pass`/`get_backup_pass`), `core/validate.sh` (`validate_env`) — **live modules (P0-K)**, each sourced by `actools.sh` at the exact spot its inline block occupied; every function body extracted **verbatim** (per-function byte-identity verified). Top-level spine code stays inline in `actools.sh`: the secret-generation calls + the v9.2-fix7 writeback loop (order unchanged), the S3 gate with the v14 default **`${ENABLE_S3_STORAGE:-true}`**, S3 provider auto-detection, and the XeLaTeX/env-mode/disk checks | none — the **stale v9.2 orphan twins are retired (P0-K)**: `validate_s3` (`${ENABLE_S3_STORAGE:-false}`), `detect_s3_provider`, `validate_xelatex`, `validate_environment_mode`, `validate_disk`, `writeback_secrets`, and the state-side `get_db_pass`/`get_backup_pass` twins did **not** survive | The stateless core as live modules with the inline copies deleted (P0-K spec) | **modules = live (P0-K landed)** — exactly-once definitions enforced by the duplicate-function guard (closure + wired-twin + unconditional twin-ban arms); orphan-content bans in `tests/core/*`; golden drift 6/6 (no fixture modified) |
| **Init** | `installer/init.sh` (invoked by `actools.sh`) — parses `--profile`/`--profile=*`, **and (P0-E) `--actor-id`/`--change-ticket`**; **sources `installer/profile.sh`** for the chosen profile; validates the **`.profile` file exists** and **fails before persisting** `actools.env` (exit 3 when absent); enforces `PROFILE_REQUIRES_ACTOR`/`PROFILE_REQUIRES_CHANGE_TICKET` via the accessors; consumes `PROFILE_INIT_FIELDS`; persists `ACTOOLS_PROFILE` (governance identity validated, **not** persisted) | `core/secrets.sh` is **named only in a comment** (`init.sh:12`), **not sourced** | `installer/init.sh` — profile-aware: source `profile.sh`, validate **file existence**, enforce actor/ticket, consume `PROFILE_INIT_FIELDS` (LOCKED §6 #2-3) | **P0-E landed (alignment §4.3)** — file-existence validation + governance enforcement + init-field consumption are wired; `community` byte-identical. **P0-H** confirmed `init` is profile-aware and added a fake-profile init-field test: a profile-declared **extra** `PROFILE_INIT_FIELDS` entry flows through the consumption loop and is **collected as a no-op (validated-not-persisted)** in Phase 0 — `init.sh` is **byte-identical to P0-E** (no edit). The install-spine *selection* of a non-default profile (sourcing the selected profile in `actools.sh::main`, vs the hardcoded `community.profile`) is a **separate** concern outside this change (`actools.sh` byte-identical) |
| **Profile loading** | `installer/profile.sh` — loads `profiles/${ACTOOLS_PROFILE}.profile` (`:21`), `exit 1` on missing file (`:28`); accessors **already exist**: `profile_requires_actor` (`:38`), `profile_requires_change_ticket` (`:39`), `profile_init_fields` (`:40`). **(P0-E) now consumed by `init`** (sourced for the chosen profile; accessors drive governance enforcement) | `profiles/README.md` narrative; `profiles/community-plus.profile` is **absent** (Phase-1 product). A **loadable test profile** now exists as the test-only fixtures `tests/fixtures/profiles/fake-{actor,ticket}.profile`, staged as `profiles/test.profile` in a sandbox `INSTALL_DIR` | `installer/profile.sh` + the resolver layer (LOCKED §2 Decision 1) | **P0-E** — loader + accessors are now **consumed by `init`**; a side-effect-free-loading test guards the "variables only" contract. Live install-path consumption (sourcing the *selected* profile in the install spine) → P0-H |
| **Install-stage orchestration** | `actools.sh::main()` (fresh) — **dispatcher-driven** (P0-D): sources `community.profile` then `for stage in "${PROFILE_INSTALL_STAGES[@]}"; do actools::dispatch::run_install_stage "$stage"; done`; trailing `setup_backup_cron`/`setup_cli`/`tls_check` unchanged. `run_install_stage`/`resolve_install_stage` live in `installer/dispatch.sh` (appended after `:191`) | `profiles/community.profile:28` defines `PROFILE_INSTALL_STAGES=(host stack db drupal worker)` — **now looped by `main()`** | A stage dispatcher: `main()` iterates `PROFILE_INSTALL_STAGES` via `run_install_stage`/`resolve_install_stage` (LOCKED §6 #5, Decision 3 append-only) | **wired (behaviour-preserving)** — P0-D landed; **P0-G wired `host`**. Stage→handler: `host`→the seven `modules/host/*` functions (P0-G, canonical order); `stack`→`setup_stack` (now delegates to the four `modules/stack/*` generators, P0-G); `drupal`→verbatim `install_env` loop; `db`/`worker`→documented no-ops still folded. Golden 6/6 unchanged. Profile **selection** by `ACTOOLS_PROFILE` → P0-E |
| **Host setup** | `modules/host/{packages,age,kernel,swap,firewall,docker,logrotate}.sh` — **live**, sourced by `actools.sh` and driven by the dispatcher's `stage_host` handler in the canonical 7-step order (`install_packages → setup_age_keypair → tune_kernel → configure_swap → configure_firewall → install_docker → configure_logrotate`) | none — the inline host block in `setup_stack` is **deleted (P0-G)**; the modules are no longer orphans | `modules/host/*` reached **through the dispatcher** (LOCKED §5/§6) | **modules = live (P0-G landed)** — host logic byte-identical in `modules/host/*`; `docker.sh`'s `local bashrc` is the one intentional adaptation. Host steps are fresh-install-only (see release note). Golden drift 6/6 (output byte-identical) |
| **Stack setup** | `modules/stack/{mycnf,images,caddyfile,compose}.sh` — **live**; `setup_stack()` (`actools.sh:430-483`, now a ~53-line thin orchestrator) sources them and calls `generate_mycnf` → `build_caddy_image`/`build_php_image`/`build_worker_image` → `generate_caddyfile` → `generate_compose`, then `docker compose pull/down/up` | none — the ten inline heredocs are **relocated into the modules (P0-G)**; the modules are no longer orphans | `modules/stack/*` **through the dispatcher**, with golden fixtures (LOCKED §5/§6) | **modules = live (P0-G landed)** — each generator body byte-identical to its monolith block (one documented `compose.sh` SC2034 comment, no output impact). Golden drift 6/6 (six files byte-identical) |
| **DB access layer (root exec / dump / backup user / readiness wait / credential probe)** | `modules/db/core.sh` — **live module (P0-M)**, sourced by `actools.sh` at the exact spot the inline blocks (`:450-530`) occupied; `db_exec_root`, `db_exec_root_stdin`, `db_dump_container`, `setup_backup_db_user`, `wait_db`, `check_db_creds` extracted **verbatim** (per-function byte-identity verified; the `tests/db/` contract/mock suite pinned the commands and SQL against the inline code first and stayed green across the move). ONE intentional post-extraction change (isolated, e2e-gated commit): `wait_db`'s readiness probe no longer passes the DB **root** password on argv — it uses a umask-077 `--defaults-extra-file` temp file inside the container, fed over stdin by the printf builtin (the backup-cron pattern); probe SQL (`mysql.actools_write_check` write-check), 50×3s bounds and outcome unchanged. SECURE SHAPE CI-locked: `tests/guards/wait_db_security_guard_test.bats` (non-vacuous) | none — the **stale v9.2 twins are deleted (P0-M)**: `modules/db/{backup_user,credentials,wait}.sh` were unwired orphans whose content (a divergent `check_db_creds` error message; the argv-password `wait_db`) did **not** survive (grep-proof: no references on any `.sh`/`.yml`/`.bats` surface). The duplicate-function guard now covers the six DB names (closure exactly-once + wired-twin + unconditional `modules/db` twin-ban arms, all non-vacuous) | The DB access layer as a live module produced from the inline v14 code, with the orphan twins purged and the argv-password probe retired (P0-M spec) | **module = live (P0-M landed)** — six functions exactly-once on the live path; `wait_db` argv exposure (Entry-017 known risk, `:510`) **CLOSED — e2e-confirmed** (run #75 reached `MariaDB ready.`; stamped at the P0-N ratification of Entry 018); golden drift 6/6 + cron fixture (no fixture modified). **(P0-N) single authority across BOTH runtimes:** the live CLI (`doctor`) now also resolves the DB layer from this module — `doctor.sh`'s byte-identical local `db_exec_root` copy is deleted and the live-CLI-path guard bans any redefinition (see the Doctor row) |
| **DB provisioning** | `actools.sh::install_env()` — inline `db_exec_root` SQL creating DB/user/grants (the *callers* stay inline; the called DB-layer functions are the P0-M module above) | none — the former `modules/db/*` v9.2 twins are **deleted (P0-M)**; `modules/db/core.sh` (the live DB access layer) is what `lint.yml`'s `modules/db/*.sh` shellcheck glob now matches | `modules/db/*` (or `db` stage handler) via dispatcher | **current (monolithic)** in `install_env`. P0-D wired the `db` stage as a documented **no-op** handler (DB creation stays inside the `install_env` loop, which runs at the `drupal` stage). **P0-M moved only the DB access layer** (the six functions); the genuine db/drupal split + `install_env` extraction is **later scope** (the post-closure renumbering made P0-N the live-CLI DB convergence — see the Doctor row) |
| **Drupal provisioning** | `modules/drupal/provision.sh` — **sourced LIVE** at `actools.sh:183`; `drupal_provision "$env"` called from `install_env` (`:~1152`) | none known | `modules/drupal/provision.sh` via dispatcher | **live (module)** — the *one* module sourced on the live path; the cleanest precedent for the extraction pattern |
| **Worker provisioning** | worker **image** built by `modules/stack/images.sh::build_worker_image` (**live**, P0-G); worker **service** rendered by `modules/stack/compose.sh::generate_compose`; worker CLI ops are **inline in `cli/actools`** (`worker-logs` :103) — the dead `cli/commands/worker.sh` twin was **deleted in P0-O** | `modules/worker/*` was **deleted in C2** (orphan — never on the live path; its `lint.yml` shellcheck line was removed with the dir); the worker **runtime** stays folded in the compose generator | `modules/worker/*` (or `worker` stage handler) via dispatcher | **partial (P0-G)** — the worker **image build** moved to `modules/stack/images.sh`; the worker **runtime**/service stays folded and the `worker` stage remains a no-op (genuine worker-stage extraction is later scope) |
| **Backup-cron generator** | `modules/backup/cron.sh::setup_backup_cron` — **live module (P0-L)**, sourced by `actools.sh` at the exact spot the inline block (`:584-661`) occupied; the function body extracted **verbatim** (per-function byte-identity verified; the generated `/etc/cron.daily/actools-backup` is **byte-identical** — golden capture `tests/fixtures/golden/backup-cron`, sha `bdfaa0c6…`). SECURE SHAPE, now CI-locked: the cron writes `[mariadb-dump]`/user/password into a **umask-077 temp file inside the DB container** and runs `mariadb-dump --defaults-extra-file="$t"`; the password is read from `.actools-state.json` at cron **runtime** — never on argv, never baked into the script (`tests/guards/cron_security_shape_guard_test.bats`, non-vacuous) | none — the **insecure orphan `cron/backup.sh` is deleted (P0-L)**: its `-ubackup -p"${BACKUP_PASS}"` put the DB password on argv (visible in `ps`); it was unwired and its form did **not** survive (grep-proof: no references on any `.sh`/`.yml`/`.bats` surface). The remaining `modules/backup/*` files are untouched P0-O-audit orphans | The backup-cron generator as a live module produced from the secure inline shape, with the insecure orphan purged (P0-L spec) | **module = live (P0-L landed)** — generated cron byte-identical (`tests/generated/backup_cron_drift_test.bats`); security shape guarded in CI; golden drift 6/6 (no compose fixture modified) |
| **CLI install** | `actools.sh::setup_cli()` (`:1247-1262`) — **install-by-copy** (P0-F): `install -m 0755 "${INSTALL_DIR}/cli/actools" /usr/local/bin/actools`; then `chmod +x` and persist `ACTOOLS_HOME=${INSTALL_DIR}` to `/etc/environment`. The old `cat > /usr/local/bin/actools <<HELPER` generator is **deleted**. | `cli/actools` (v14.0, profile-aware static CLI) + `cli/commands/*.sh` — **now the single canonical source**. Resolves `INSTALL_DIR="${ACTOOLS_HOME:-<self-locate>}"` (`:7`) so the verbatim copy works at `/usr/local/bin/actools`. DR still copies it independently (`modules/dr/resurrect.sh:153` → `actools-real`). The former false comment at `cli/actools:12-15` is **rewritten** to describe install-by-copy. | **One** canonical source — `cli/actools`, copied to `/usr/local/bin/actools`; the `HELPER` heredoc deleted (LOCKED §5; ROADMAP `ROADMAP.md:105-120`) | **consolidated (P0-F landed)** — single source; installed CLI proven byte-identical by `tests/installer/cli_authority_test.bats`; false comment rewritten. DR `actools-real` copy left for P0-J. |
| **Preflight** | `installer/preflight.sh` — routes each `PROFILE_PREFLIGHT_EXTRA` entry through **`actools::dispatch::resolve_profile_check "preflight"`** (P0-H): a resolved+installed handler runs (its non-zero return counts a failure); an extra **declared but with no installed handler is a hard FAIL for a non-default profile** (`print_fail`+`print_fix`, replacing the old `print_skip`). `community`'s list is empty, so the loop body never executes (byte-identical) | profile `PROFILE_PREFLIGHT_EXTRA` arrays — **now dispatched** | resolver-backed `preflight`: route extras via `resolve_profile_check "preflight"`; **fail unknown for non-default** (LOCKED §6 #4) | **consumed (P0-H landed)** — resolved runs, unknown fails; `community` byte-identical (preflight_test 6/6; drift 6/6) |
| **Doctor** | `cli/commands/doctor.sh` — `--deep` is resolved through **`actools::dispatch::resolve_feature_handler doctor_deep`** (P0-H): a non-`community` profile's override (3-tier) is sourced when present; `community` short-circuits to empty so the gate **falls back to the built-in `cli/commands/doctor_deep.sh`** (in-development gate, exit 2) — **no hardcoded handler path remains**. env + `dispatch.sh` are sourced at the top of `run_doctor` so the resolver is available to the gate; prints active profile. **(P0-N)** the DB layer converged onto the P0-M authority: `doctor.sh` sources `modules/db/core.sh` at the exact top-level spot its local `db_exec_root` copy (byte-identical dual truth) occupied — the copy is **deleted**; the source is best-effort (`2>/dev/null \|\| true`, the file's minimal-sandbox contract), with the live resolution pinned typo-proof by `tests/cli/doctor_db_authority_test.bats` and any live-CLI redefinition banned by `tests/guards/cli_db_authority_guard_test.bats` (both non-vacuous) | only the static `doctor` path (P0-F); `PROFILE_DOCTOR_EXTRA` still absent | resolver-backed `doctor`: replace the hard `source doctor_deep.sh` with `resolve_feature_handler` (LOCKED §4.1) | **deep handler consumed (P0-H landed)**; `community` byte-identical (doctor_test 5/5). **DB layer single-authority (P0-N landed)** — `db_exec_root` (and the five inert siblings) resolve to `modules/db/core.sh` on the live CLI path; doctor output unchanged (verbatim-equivalent swap; the `:160` call site is byte-untouched). **Per-check `PROFILE_DOCTOR_EXTRA` loop deliberately deferred** — `resolve_doctor_check` exists + is tested; no Phase-0 consumer (community-plus/Phase-1) |
| **Handoff** | `installer/handoff.sh` — built-in sections `(site admin commands log)`; the former **silent `*)`** now routes non-built-in sections through **`actools::dispatch::resolve_handoff_section`** (P0-H): a resolved+installed handler renders the section; an **unresolved section emits a visible (non-fatal) notice** — handoff is a display surface, so unlike preflight an unresolved section is reported, not failed. `community` sections all hit explicit arms → `*)` never fires (byte-identical) | neither CLI exposes `handoff` (installer concept, P0-F) | resolver-backed `handoff`: `resolve_handoff_section` instead of the silent `*)` (LOCKED §6 #7) | **consumed (P0-H landed)** — resolved renders, unresolved is a visible notice; `community` byte-identical (drift 6/6) |
| **Resolver layer (the seam)** | `installer/dispatch.sh` — guard `_ACTOOLS_DISPATCH_SOURCED` (`:27-28`). **(P0-E)** `resolve_feature_handler` now returns a **PATH via 3-tier resolution** — Tier 1 `profiles.d/${ACTOOLS_PROFILE}/commands/${feature}.sh` → Tier 2 `modules/${module}/${feature}.sh` (via internal `PROFILE_FEATURE_MODULES`) → Tier 3 `cli/commands/${feature}.sh`; first existing wins, else empty; **`community` short-circuits to empty** (byte-identical). **(P0-E)** `resolve_profile_check <surface> <check_id>` umbrella **added** (delegates to the per-surface internals). The per-surface `resolve_preflight_check`/`resolve_doctor_check`/`resolve_handoff_section` are token-based; **`resolve_preflight_check` and `resolve_handoff_section` are now consumed live (P0-H)** via the `resolve_profile_check` umbrella, and `resolve_feature_handler` drives doctor `--deep`; `resolve_doctor_check` remains **uncalled** (its per-check consumer loop is deliberately deferred — see the Doctor row). `resolve_install_stage`/`run_install_stage` + `actools::install::stage_*` (P0-D) remain the only resolver pair **called on the live install path** | the bats fixture `tests/fixtures/profiles/test/*` exercises the token resolvers **in isolation**; a sandbox `INSTALL_DIR` + the `fake-*` fixtures exercise the 3-tier resolver and governance **in isolation** | `resolve_feature_handler` implementing the **3-tier path order**; a `resolve_profile_check <surface> <check_id>` umbrella delegating to the per-surface internals (LOCKED §2 Decision 1; §6 #1) | **P0-E landed** the 3-tier `resolve_feature_handler` (alignment §4.1) + the `resolve_profile_check` umbrella (alignment §4.2), as **internal primitives with no live call sites**. Token→path is a deliberate contract change, safe only because there are 0 live callers; **the surfaces are now wired (P0-H)**: `resolve_feature_handler` drives doctor `--deep`, `resolve_profile_check` drives preflight extras and handoff sections, and `community` short-circuits to empty everywhere (drift 6/6) |
| **Generated compose** | `modules/stack/compose.sh::generate_compose` (**live**, P0-G) — incl. all-in-one/redis/cadvisor fragments + the preserved redis-off `depends_on` quirk | none (the orphan was replaced with the monolith's exact bytes) | module + **golden fixtures** (LOCKED §6; generated-file contract) | **module = live (P0-G landed)**; byte-identical output (golden drift 6/6); one documented SC2034 false-positive comment |
| **Generated Caddyfile** | `modules/stack/caddyfile.sh::generate_caddyfile` (**live**, P0-G) — full security headers + `/health`/`/csp-violations` + `@login rate_limit` + embedded all-in-one fragment | none (orphan replaced with monolith bytes) | module + golden fixtures | **module = live (P0-G landed)**; byte-identical output (golden drift 6/6) |
| **Generated my.cnf** | `modules/stack/mycnf.sh::generate_mycnf` (**live**, P0-G) — env-default `${INNODB_BUFFER_POOL:-1G}` | none (orphan replaced with monolith bytes) | module + golden fixtures | **module = live (P0-G landed)**; byte-identical output (golden drift 6/6) |
| **Generated Dockerfiles** | `modules/stack/images.sh::build_caddy_image`/`build_php_image`/`build_worker_image` (**live**, P0-G) — caddy heredoc QUOTED; php keeps the `if [[ ! -f Dockerfile.php ]]` guard; worker `--build-arg` | none (orphan replaced with monolith bytes); note `docs/CHANGELOG.md:113`'s old "template variables" claim remains inaccurate (heredocs, now in a module) — CHANGELOG cleanup is P0-B/P0-J | module + golden fixtures | **module = live (P0-G landed)**; byte-identical output (golden drift 6/6) |
| **~~Generated CLI~~ (retired P0-F)** | ~~`actools.sh::setup_cli()` `HELPER` heredoc~~ — **removed**; `setup_cli` now copies `cli/actools` verbatim | `cli/actools` (static, **now canonical**) | one source (`cli/actools`); CLI is no longer generated, so it has **no golden fixture** — integrity is proven by `tests/installer/cli_authority_test.bats` (installed == source) | **consolidated (P0-F landed)**. The six non-CLI generated files (`my.cnf`, `Dockerfile.{caddy,php,worker}`, `Caddyfile`, `docker-compose.yml`) are unaffected; golden drift 6/6. |

> **Command authority (P0-O).** The authoritative list of real `actools`
> subcommands is `cli/actools`'s own dispatch — every command is inline.
> `cli/commands/` is **not** a command registry: after the P0-O dead-twin
> deletion it holds only the two live handler files `doctor.sh` +
> `doctor_deep.sh` (sourced on demand via the resolver, P0-H). The eight dead
> twin files (`backup`, `ci_generate`, `cost_optimize`, `health`, `restore`,
> `storage`, `update`, `worker`) were deleted in P0-O — none was ever a
> registered command (their `cmd_*` functions were called 0× in `cli/actools`).

## Verified secondary facts (carried into later phases)

- **Profile-/resolver-blindness of the live spine — partially closed (P0-D).** Before P0-D, `actools.sh` had `ACTOOLS_PROFILE` = 0 refs, `PROFILE_INSTALL_STAGES` = 0, `resolve_` = 0, `dispatch::` = 0. P0-D wired `main()` (fresh) to `source community.profile` and call `actools::dispatch::run_install_stage` over `PROFILE_INSTALL_STAGES`, so the install spine now reaches the dispatcher. Profile **selection** via `ACTOOLS_PROFILE` (vs the current hardcoded `community.profile` source) remains P0-E; the other surfaces (preflight/doctor/handoff) remain resolver-blind on the live path. **(P0-H closed the surface path — preflight/doctor/handoff now consume the resolvers; the install-spine profile *selection* is the only remaining resolver-blind spot.)**
- **Secret-ordering hazard (preserved in P0-G; still to encode as an explicit guard).** `gen_if_empty DB_ROOT_PASS` precedes `MARIADB_ROOT_PASSWORD: "${DB_ROOT_PASS}"` rendered by the compose generator; the file carries `[fix4]/[fix7]` scars. **P0-G preserved this ordering**: the `stack` stage runs after secret generation, and `modules/stack/compose.sh::generate_compose` reads `DB_ROOT_PASS` from the environment at render time (the extraction did not reorder secret-gen vs render). Any future stage that renders before secret-gen would reintroduce an empty root password — an explicit guard is still owed (later scope).
- **Test surface = 138 bats tests** (`tests/test_d0_dispatch.bats` **48 (P0-E: 33 + 15)**, `tests/installer/init_test.bats` 11, `tests/installer/init_profile_test.bats` **10 (P0-E)**, `tests/installer/preflight_test.bats` 6, `tests/installer/doctor_test.bats` 5, `tests/installer/dispatch_stages_test.bats` **14 (P0-D 12 + P0-G 2)**, `tests/installer/cli_authority_test.bats` **14 (P0-F)**, `tests/core/validate_test.bats` 11, `tests/core/secrets_test.bats` 10, `tests/test_p0h_dispatch.bats` **9 (P0-H)**), plus the `tests/generated/golden_drift_test.bats` 6-test drift gate (**144 total** at P0-H; **P0-I → 158**: +13 in the new `tests/test_p0i_fake_profile_e2e.bats` and +1 resolver-bypass audit in `tests/test_d0_dispatch.bats`, which is now 49). `docs/architecture.md:49` ("21 bats tests") is stale.
- **Test surface — P0-K update.** 158 → **192** bats tests: +5 in `tests/guards/*` (the live-authority guard, the duplicate-function guard's three arms, and the closure-sanity pin), and `tests/core/*` rebuilt from 21 stale-orphan tests to **50** live-code tests (`bootstrap_test` 12, `state_test` 10, `secrets_test` 17, `validate_test` 11) — behavior captured against the inline v14 code first (commit 2), then re-pointed at each live module in its extraction commit with the same assertions. The drift gate stays 6 tests / 6 files per variant manifest.
- **Test surface — P0-L update.** 192 → **199** bats tests: +3 in the new `tests/generated/backup_cron_drift_test.bats` (byte-compare re-render against the committed cron fixture; manifest self-consistency; no-secret pin) and +4 in the new `tests/guards/cron_security_shape_guard_test.bats` (defaults-extra-file required; argv-password banned; a permanent non-vacuity arm; a generator-source check). The renderer is `tests/helpers/capture_backup_cron.sh` (fixed inputs; pins the `/etc/cron.daily/actools-backup` target and substitutes only the output location — heredoc bytes untouched). The compose drift gate stays 6 tests / 6 files per variant manifest, fixtures unmodified.
- **Test surface — P0-M update.** 199 → **216** bats tests: +13 in the new `tests/db/db_contract_test.bats` (contract/mock pins over a PATH-interposed `docker` stub — `tests/db/mock_docker.bash` — with the auto-locating loader `tests/db/db_layer_loader.bash`; the same assertions ran green against the inline code and the extracted module, which is the faithfulness proof for a stateful layer with no golden-capturable output) and +4 in the new `tests/guards/wait_db_security_guard_test.bats` (secure `--defaults-extra-file` shape required; argv-password banned on executable text; a permanent non-vacuity arm; a behavioral mock arm proving the password travels stdin-only). The duplicate-function guard's three arms now cover sixteen names (ten stateless-core + six DB). The compose drift gate stays 6 tests / 6 files per variant manifest and the cron drift gate 3 tests, fixtures unmodified.
- **Test surface — P0-N update.** 216 → **230** bats tests: +7 in the new `tests/guards/cli_db_authority_guard_test.bats` (the live CLI path — `cli/actools` + its sourced `cli/commands/*` targets — must define none of the six DB names; authority excluded by construction; three permanent non-vacuity fixture arms; dead twins naturally excluded, green pre/post-P0-O) and +7 in the new `tests/cli/doctor_db_authority_test.bats` (+ `tests/cli/doctor_loader.bash`: no local def; authority source line present; inert sourcing; source-time `declare -f` byte-equal to `core.sh`; the no-module non-vacuity twin that makes the best-effort source typo-proof; the P0-M mock-docker oracle). Drift 6/6 + cron 3/3 unchanged, fixtures unmodified.
- **Resolver-bypass guard — extended (P0-I).** The sibling-scope audit in
  `tests/test_d0_dispatch.bats` ("every `ACTOOLS_PROFILE` reader sources
  `dispatch.sh` or is `DISPATCH_EXEMPT`") is **preserved**, and **P0-I added a
  companion resolver-bypass audit** (alignment §4.4 / LOCKED §10 Risk 2): no file
  other than the resolver (`installer/dispatch.sh`) may `source`/`.` a
  `${INSTALL_DIR}/modules/plus_*` path. Clean at baseline; it trips if a
  hardcoded plus-path bypass is introduced in Phases 1–6.
- **CI gaps — closed (P0-I).** `.github/workflows/lint.yml`'s bats job ran a
  6-file subset; **P0-I switched it to the full recursive suite**
  (`bats --print-output-on-failure -r tests/`, 158 tests, auto-discovering future
  suites) and **added `actools.sh` to shellcheck**
  (`--exclude=SC2034,SC2015,SC2164,SC1091`, documented; **no `actools.sh` edit** —
  flag-don't-edit). `.github/workflows/e2e.yml:84` previously ran `./actools.sh
  install` with no profile **and** masked a failed install via `tee`; **P0-I
  fixed the tee exit-masking** (`pipefail` + `if !` guard) and **added a hermetic
  `fake-profile-e2e` job** that drives every dispatch point under
  `ACTOOLS_PROFILE=test` (the right shape of e2e for what Phase 0 hardened — the
  seams; a fake-profile *VM* install stays out of scope, as it would require the
  deferred install-spine profile selection). S2 satisfied.
- **Doc contradictions (P0-B/P0-J).** `docs/architecture.md`: "v11.2.0+" (`:3`), "the CLI … never contains business logic" (`:9`), "21 bats tests" (`:49`), `"version":"11.2.0"` + `phases_complete` state machine (`:84-87`), `actools-real` (`:119`) — all false vs code. `docs/CHANGELOG.md:113` Dockerfile claim false. `cli/actools:12-15` false self-comment.
- **Design-canon home (build-trigger #2)** was **absent** before P0-A (`design/` did not exist; LOCKED/brief/arch not committed under `docs/` or `design/`). P0-A creates `design/` with the three canon files.

## Standalone modules (live vs quarantined)

> **Recorded at C1** (doc + guard only). **Amended C2** (dead-twin removal) and
> **C3** (4.5-seed quarantine into `experimental/`). Human-readable mirror of
> `tests/guards/orphan_inventory_guard_test.bats`, which pins the live-module set
> **and** that nothing under `experimental/` is ever sourced. **Any phase that
> changes the live-module set must update BOTH** this table and that guard's
> `EXPECTED_LIVE_MODULES`. Baseline `8c1897c`.

After C3, `modules/` holds **exactly the 6 LIVE modules** — those reached by the
live install path (sourced from `actools.sh`, or referenced by the installer /
operator CLI). The **7 4.5-design seeds** that previously sat in `modules/` as
orphans (no live reference) were **moved to `experimental/`** in C3 — `git mv`,
content byte-identical, history preserved. (The 5 dead-twins — `health, migrate,
preflight, storage, worker` — were deleted in C2.)

**This is a surface quarantine, not physical removal.** The install is in-place
(`INSTALL_DIR` = the repo dir, `actools.sh:94`) and `chown -R`'s the whole tree
(`actools.sh:405`), so the `experimental/` files still reside on the box — but
they are off the live surface, and the guard fails CI if any `experimental/…`
path is ever reached by the live closure or wired into an entry point. The
"unwired design reference" framing the operator docs already used (see
`enterprise.md`) is unchanged; only the paths moved to `experimental/`.

**Totals: 6 live (`modules/`) · 7 quarantined 4.5-seed (`experimental/`).**
(Lineage: 18 module dirs at C1 → 12 orphan / 6 live; C2 deleted 5 dead-twins and
reclassified `ai`+`preview` as seeds → 13 dirs, 7 orphan; C3 moved the 7 seeds
to `experimental/` → `modules/` = 6, `experimental/` = 7.)

| module | location | status | evidence |
|---|---|---|---|
| `audit` | `modules/` | LIVE | live path — `cli/actools:313,317` (operator-CLI surface, installed by copy) |
| `backup` | `modules/` | LIVE | live path — `actools.sh:516` (`modules/backup/cron.sh`) |
| `db` | `modules/` | LIVE | live path — `actools.sh:457` (`modules/db/core.sh`) |
| `drupal` | `modules/` | LIVE | live path — `actools.sh:181` (`modules/drupal/provision.sh`) |
| `host` | `modules/` | LIVE | live path — `actools.sh:193` loop over `modules/host/*.sh` |
| `stack` | `modules/` | LIVE | live path — `actools.sh:204` loop over `modules/stack/*.sh` |
| `ai` | `experimental/` | quarantined 4.5-seed | no live reference (C3 move) |
| `compliance` | `experimental/` | quarantined 4.5-seed | no live reference (C3 move) |
| `dr` | `experimental/` | quarantined 4.5-seed | no live reference (C3 move) |
| `network` | `experimental/` | quarantined 4.5-seed | no live reference (C3 move) |
| `observability` | `experimental/` | quarantined 4.5-seed | no live reference (C3 move) |
| `preview` | `experimental/` | quarantined 4.5-seed | no live reference (C3 move) |
| `security` | `experimental/` | quarantined 4.5-seed | no live reference (C3 move) |

The LIVE rows are exactly the guard's `EXPECTED_LIVE_MODULES`
(`audit backup db drupal host stack`). The guard **derives** the actual set from
the tree — the source-closure of `actools.sh` (the `CLOSURE` engine from
`live_closure.bash`) **unioned** with `modules/<name>` references in `actools.sh`
/ `installer/` / `cli/actools` — and fails CI if it diverges in either
direction. A separate arm fails if any `experimental/…` path enters the live
closure. `audit` is the one live module reached without an `${INSTALL_DIR}`
source line from `actools.sh`: it is invoked from `cli/actools` (the copied
operator-CLI surface), which is why the union with the entry-point grep, not the
closure alone, is required.

## Update rule

Every phase must update this map if it changes authority.

## Review question

Before merge, ask:

> Did this phase change which file is authoritative for any runtime behavior?

P0-A answer: **No.** P0-A added `docs/`+`design/` content only; no `current` authority moved. The first authority move is **P0-D** (install-stage orchestration: `actools.sh::main()` hardcoded sequence → stage loop, behaviour-preserving). Update this map and the ledger at that phase.

P0-D answer: **Yes — and this is the first such move.** Install-stage orchestration authority moved from the hardcoded `main()` sequence to the dispatcher in `installer/dispatch.sh` (`run_install_stage`/`resolve_install_stage` + `actools::install::stage_*`), with `main()` now iterating `PROFILE_INSTALL_STAGES`. The move is **behaviour-preserving** (golden drift 6/6 unchanged; handlers call `setup_stack`/`install_env` verbatim; `host`/`db`/`worker` are no-ops until P0-G). No generated-file authority and no CLI authority moved. Map + ledger (Entry 009) updated in the same commit.

P0-E answer: **Yes, for one concern — `init`.** Profile-validation authority moved: `installer/init.sh` now sources `installer/profile.sh`, validates the profile **file exists**, and enforces governance flags **before persisting** `actools.env` (previously it validated list membership only). This is the only **live** authority change. The resolver-layer contract was also completed — `resolve_feature_handler` is now 3-tier **path** resolution and a `resolve_profile_check` umbrella was added — but those are **internal primitives with no live call sites** (their callers are P0-H), so no install-path authority moved and **`actools.sh` is byte-identical**. Community behaviour is byte-identical; golden drift 6/6 unchanged. Map + ledger (Entry 010) updated in the same commit.

P0-G answer: **Yes — and it is the largest authority move so far.** Host provisioning and stack generation moved out of the monolithic `actools.sh::setup_stack()` into the canonical modules: `modules/host/*` (driven by the dispatcher's `stage_host` handler in the canonical 7-step order) and `modules/stack/*` (`generate_mycnf`, `build_caddy_image`/`build_php_image`/`build_worker_image`, `generate_caddyfile`, `generate_compose`), which `setup_stack` — now a ~53-line thin orchestrator — sources and calls before `docker compose pull/down/up`. The previously orphaned modules were overwritten with the monolith's exact bytes and are now the live authority; `actools.sh` shrank 1416 → 871 lines. The move is **behaviour-preserving**: golden drift 6/6 with **no fixture modified** (the six generated files are byte-identical), and the only deviations are the documented `compose.sh` SC2034 false-positive comment (no output impact) and the deliberate host-stage behaviour differences (host steps are now fresh-install-only — dry-run/interactive-N/update/env — see the release note). **DB** user/credential creation and the **worker runtime** stay folded (their stages remain no-ops; only the worker **image** build moved). No CLI authority moved (P0-F unchanged). Map + ledger (Entry 012) updated in the same commit.

P0-I answer: **No.** P0-I added **tests + CI only** — no `current` authority moved. The seam (`installer/dispatch.sh`), the surfaces, and `actools.sh` are **byte-identical**, and `community` is byte-identical (golden drift 6/6). What changed is *coverage*: a **loadable** `profiles/test.profile` plus `tests/test_p0i_fake_profile_e2e.bats` drive **every dispatch point** under `ACTOOLS_PROFILE=test` — the seam, end-to-end, **hermetically** (the right shape of "e2e" for what Phase 0 hardened; a fake-profile VM install would require the deferred install-spine selection + stub handlers that cannot build a working site). CI now gates the **whole** bats suite, **shellchecks `actools.sh`** (documented exclusions; no edit), runs a hermetic **`fake-profile-e2e`** job, and **fixes the `e2e.yml` `tee` exit-masking**; the §4.4 sibling-scope audit gains a companion **resolver-bypass audit** (LOCKED §10 Risk 2). Map + ledger (Entry 014) updated in the same commit.
P0-K answer: **Yes — four concerns moved, plus the guards that make the next wrong move fail CI.** The stateless core moved out of inline `actools.sh` into the canonical modules, which are now the live authority: `core/bootstrap.sh` (`log`/`warn`/`error`/`section`/`dryrun`), `core/state.sh` (`init_state`/`set_state`/`get_state`/`is_installed`/`mark_installed`), `core/secrets.sh` (`rand_pass`/`gen_if_empty`/`get_db_pass`/`get_backup_pass`), `core/validate.sh` (`validate_env`) — one unit per commit (bootstrap → state → secrets → validate), each overwriting its stale v9.2 orphan twin with the **verbatim inline v14 implementation** and deleting the inline copy in the same commit, so no dual definition ever existed on the live path. The move is **behaviour-preserving**: golden drift 6/6 at every commit with no fixture modified (`actools.sh` 871 → 835 lines); the v14 traps held — `INSTALL_DIR` stays BASH_SOURCE-relative (the orphan's `$REAL_HOME` form is dead), the S3 gate stays top-level with `${ENABLE_S3_STORAGE:-true}` (the orphan's `:-false` is dead), the jq/state-file semantics and the fix7 secret-writeback order are unchanged. What deliberately did **not** move: the top-level spine code (DRY_RUN init, secret-gen calls + writeback loop, S3 gate/detection, XeLaTeX/env-mode/disk checks), `main()`'s profile source (P0-P), and the DB/install_env/cron/CLI layers (P0-L/M/N). Two anti-regression guards entered CI via the recursive bats job: the **live-authority guard** (a `LIVE AUTHORITY` claim requires presence on the live source-closure) and the **duplicate-function guard** (each of the ten risky core functions defined exactly once on the live path; hardened at the final commit with an unconditional `actools.sh`+`core/*.sh` twin ban) — both proven non-vacuous (three captured failure demos in `docs/tests/P0-K-guards-and-stateless-core.md`). Map + ledger (Entry 016) updated in the phase's docs commit.

P0-L answer: **Yes — one concern moved, one orphan died, and the secure shape is now CI-locked.** The daily backup-cron generator moved out of inline `actools.sh` (`:584-661`) into `modules/backup/cron.sh`, now the live authority, sourced at the exact spot the function occupied — the function body verbatim (per-function byte-identity verified with the P0-K extraction primitive) and the generated `/etc/cron.daily/actools-backup` **byte-identical** (the golden fixture was captured from the inline generator BEFORE the move and the same renderer, now locating the module, re-renders the same sha `bdfaa0c6…`). The move is behavior-preserving: golden drift 6/6 with no compose fixture modified (`actools.sh` 835 → 763 lines). The security rule held: the live generator's secure shape — umask-077 temp defaults file inside the container, `mariadb-dump --defaults-extra-file=`, password read from `.actools-state.json` at cron runtime, never on argv — is preserved unchanged and locked by `tests/guards/cron_security_shape_guard_test.bats` (four arms; a permanent non-vacuity arm renders a doctored argv-password generator through the same pipeline and proves the oracle rejects it). The insecure orphan `cron/backup.sh` (`-ubackup -p"${BACKUP_PASS}"` — the argv-password twin Gemini's inverted P0-J recommendation would have deployed) is **deleted and unreferenced**. What deliberately did **not** move: the `db_exec_root <<SQL` DB-user heredocs and the help/version `<<EOF` (small, idiomatic, out of scope), the DB layer / `install_env` / CLI (P0-M/P0-N), the other `modules/backup/*` orphans (P0-O audit), and `main()` (P0-P). Map + ledger (Entry 017) updated in the phase's docs commit.

P0-N answer: **Yes — one resolution converged; no definition moved and no behavior changed.** The live CLI's `doctor` command stopped carrying its own copy of `db_exec_root` (`cli/commands/doctor.sh:27` — byte-identical to `modules/db/core.sh:45`, the byte-identity re-verified including the comment) and now **sources the P0-M authority** at the exact top-level spot the local def occupied, so the six DB functions are defined **only in `modules/db/core.sh` across both runtimes** (installer and CLI) — a future fix to the module reaches `doctor` automatically instead of silently missing it. The swap is verbatim-equivalent: the `:160` call site is byte-untouched (the replacement is 6-for-6 lines, so it is still literally at `:160`), the resolved function's `declare -f` body is byte-equal to the canonical one, and doctor's output is unchanged (the e2e doctor-smoke is the backstop; no new e2e gate — this is not a behavior change). One flagged deviation: the source is **best-effort** (`2>/dev/null || true`) rather than the spec snippet's bare form, because `doctor.sh` must stay sourceable in a minimal sandbox (its own `:37-39` contract; the deep-gate suites stage `INSTALL_DIR`s without `modules/` and a bare source broke 9 tests) — the typo-risk this could mask is pinned red by the focused test's resolution arms. Two new suites entered CI via the recursive bats job: `tests/guards/cli_db_authority_guard_test.bats` (the live CLI path — `cli/actools` + its sourced command files — defines none of the six names; non-vacuous via permanent fixture arms plus a captured live injection demo; the eight dead twins are naturally excluded → green pre-P0-O) and `tests/cli/doctor_db_authority_test.bats` (resolution-side pins incl. the no-module twin and the P0-M mock-docker oracle). The eight dead twins and their DB-fn copies are untouched (P0-O); `modules/db/core.sh` is untouched; drift 6/6 + cron 3/3. Map + ledger (Entry 019; Entry 018 ratified — merge SHA `cd0d0d9`/#47 stamped, the `wait_db:510` risk flipped to CLOSED — e2e-confirmed, run #75) updated in the phase's docs commit.
