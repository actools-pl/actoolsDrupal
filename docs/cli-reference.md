# CLI reference (moved)

This page has moved to [`command-reference.md`](command-reference.md).

The new page is grouped by frequency of use and includes the staged installer modes (`init`, `preflight`, `install`, `handoff`) alongside the runtime CLI.

For day-to-day operations, start at [`operator-handbook.md`](operator-handbook.md).
