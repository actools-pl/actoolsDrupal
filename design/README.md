# Design Canon — Actools Drupal Community-Plus

This directory is the **published design-canon home** required by
`Actools_Drupal_Community_Plus_LOCKED.md` §11 build-trigger condition #2. It is referenceable from
the issue tracker; link to it from the Phase-0 tracking issue.

Canon (authority order, highest first):

1. `Actools_Drupal_Community_Plus_LOCKED.md` — the design constitution. **LOCKED; never re-opened.**
2. `actools-phase0-implementation-plan.md` — the verified Phase-0 synthesis/plan (WP-*↔P0-* crosswalk in its banner). The plan P0-A **adopts**.
3. `actools-phase0-locked-alignment.md` — errata/addendum to #2; never overrides #1.

The runnable workflow package (phases, runbooks, contracts, prompts) lives separately and is
materialized into `docs/` (`docs/architecture/`, `docs/runbooks/`). The in-repo `docs/` copies are
what AI windows read; this `design/` directory is the versioned canon source.
